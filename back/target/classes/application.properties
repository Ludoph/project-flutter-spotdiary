# Configuration de la source de donn�es (base de donn�es PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5433/spotdiary
spring.datasource.username=spotdiary_admin
spring.datasource.password=patatipatata
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuration de Hibernate pour PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuration des entit�s (packages o� chercher les classes annot�es @Entity)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Configuration de la date/heure du fuseau horaire (facultatif)
# spring.jackson.time-zone=UTC

# Configuration du port du serveur (facultatif)
server.port=8080
